---
title: "Imprint"
---

This page has **no** `edit_roles` set at all, which means it's open to **any** authenticated user with repository write access — the same permission model you get by default without this hook.

`edit_roles` is opt-in: only pages that set it are restricted.
